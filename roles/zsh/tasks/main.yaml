- name: remove zsh directory
  file:
    path: "{{ home }}/.oh-my-zsh"
    state: absent
- name: Download oh-my-zsh
  git:
    repo: https://github.com/ohmyzsh/ohmyzsh.git
    dest: "{{ home }}/.oh-my-zsh"
    depth: 1
- name: copy .zshrc
  copy:
    src: ./zshrc
    dest: "{{ home }}/.zshrc"
