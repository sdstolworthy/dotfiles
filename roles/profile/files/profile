if [ -d "${HOME}/.profile.d/" ] && [ "$(ls -1 "${HOME}/.profile.d/" | wc -l)" -gt 0 ]; then
  for i in "${HOME}"/.profile.d/*; do
    if [ -r "$i" ]; then
      if [ "$PS1" ]; then
        . "$i"
      else
        . "$i" >/dev/null 2>&1
      fi
    fi
  done
fi
